#include <stdio.h>
int main(){
    int ahsan[5];
    for(int i = 0; i <5; i++)
    {
        scanf("%d", &ahsan[i]);
    }
    for(int i = 0; i<5; i++)
    {
        printf("%d\n", ahsan[i]);
    }

    return 0;
}